# Dual-Robots-Dice-clearing-system

A vision-guided automation system where a camera detects dice using OpenCV, uses homography matrix to convert pixel coordinates to robot coordinates, and estabishes communication between two robots (FANUC and StandardBot SDKs) over MQTT to pick, transfer, and stack dice.
<br>
Author - (Nishan Thapa)
