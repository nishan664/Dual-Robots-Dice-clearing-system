# Dual-Robots-Dice-clearing-system
A vision-guided automation system where a camera detects dice using OpenCV, use homography matrix to convert pixel coordinates to robot coordinates, and communicate two robots (FANUC and StandardBot SDK) over MQTT to pick, transfer, and stack dice.
